const username: string = 'MLLuisa';

const sum = (a: number, b: number) => {
  return a + b;
}

sum(2, 2);

class Person {
  constructor(public age: number, public lastName: string) {
  }
}

const mariaLuisa = new Person(27, 'Lufra');

